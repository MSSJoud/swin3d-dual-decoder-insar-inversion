services:
  inversion:
    build: .
    image: swin3d-dual-decoder-insar-inversion:latest
    volumes:
      - ./sample_data:/workspace/sample_data
      - ./runs:/workspace/runs
    working_dir: /workspace
    command: ["inspect", "--config", "configs/example_hdf5_train.json"]
